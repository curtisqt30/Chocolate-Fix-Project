
// Create the shape, the +300 is to seperate the grids so this goes down 300 pixels
lp_draw_shape(global.lp_shape_2, global.lp_x_offset, global.lp_y_offset + 300, global.lp_piece_size);

// Draw the pieces
lp_draw_pieces(global.panel2);
