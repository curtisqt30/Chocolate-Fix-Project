
// Create the shape, the +300 is to seperate the grids so this goes down 300 pixels
draw_hint_shape(global.hint_shape_2, global.hint_x_offset, global.hint_y_offset + 300, global.hint_piece_size);

// Draw the pieces
draw_hint_pieces(global.hint2);